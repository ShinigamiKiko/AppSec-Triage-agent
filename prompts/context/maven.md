# Java and Kotlin (Maven)

## Test And Non-Production Paths

File names, Java and Kotlin — the CamelCase suffix convention:

- `*Test.*`
- `*Tests.*`
- `*TestCase.*`
- `*Spec.*`

## Calls a Call Graph Misses

`Class.forName`, `Method.invoke`, `ServiceLoader`, and beans a framework wires by
annotation or XML configuration.
